
<view wx:for="{{phone_list}}" wx:for-item="phone" wx:key="_id"
  bind:click="viewItem"
  data-id="{{phone._id}}"
  bindtap="{{viweItem}}"
>
 <van-card title="{{phone.title}}"
 thumb="{{phone.image}}"
 desc="{{phone.color}}"
 price="{{phone.price}}"
 ></van-card>
</view>